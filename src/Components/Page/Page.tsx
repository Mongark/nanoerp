import { Paper } from '@material-ui/core'
import React from 'react'

const style = {
    padding: '2%',
};

const Page = ( props: any ) => {
  return (
    <div>
        <Paper style={style} />
    </div>
  )
}

export default Page;
